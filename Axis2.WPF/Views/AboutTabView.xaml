<UserControl x:Class="Axis2.WPF.Views.AboutTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="900">
    <Grid>
        <Border MaxWidth="620" VerticalAlignment="Center" HorizontalAlignment="Center"
                Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="1" CornerRadius="14" Padding="40,32" Margin="20">
            <Border.Effect>
                <DropShadowEffect Color="#22000000" BlurRadius="24" ShadowDepth="0"/>
            </Border.Effect>
            <StackPanel HorizontalAlignment="Center">
                <Image Source="/Assets/icon.png" Width="80" Height="80" HorizontalAlignment="Center"
                       RenderOptions.BitmapScalingMode="HighQuality"/>

                <TextBlock Text="Axis Sphere51" FontSize="30" FontWeight="Bold"
                           HorizontalAlignment="Center" Margin="0,18,0,2"
                           Foreground="{DynamicResource TextBrush}"/>
                <TextBlock Text="Sphere 51a toolkit"
                           FontSize="13" HorizontalAlignment="Center"
                           Foreground="{DynamicResource TextMutedBrush}"/>
                <TextBlock Text="Version 0.3"
                           FontSize="12" FontWeight="SemiBold" HorizontalAlignment="Center"
                           Margin="0,6,0,0"
                           Foreground="{DynamicResource AccentDarkBrush}"/>

                <Border Height="1" Background="{DynamicResource BorderBrush}" Margin="0,22"/>

                <TextBlock HorizontalAlignment="Center" FontSize="18" FontWeight="SemiBold"
                           Foreground="{DynamicResource AccentDarkBrush}"
                           Text="rewritten by rawedit 2026"/>

                <StackPanel Margin="0,20,0,0">
                    <TextBlock Foreground="{DynamicResource TextMutedBrush}" FontSize="12.5"
                               TextWrapping="Wrap" TextAlignment="Center"
                               Text="Based on Axis2 (WPF / .NET 9). Adds parsing of legacy Sphere 0.51a item scripts (bare-hex tile blocks), a modern interface, and .mul/.uop art rendering."/>
                    <TextBlock Foreground="{DynamicResource TextMutedBrush}" FontSize="12"
                               HorizontalAlignment="Center" Margin="0,14,0,0"
                               Text="Original visual by Prapilk • Honoring PUNT"/>
                    <TextBlock HorizontalAlignment="Center" Margin="0,16,0,0" FontSize="13">
                        <Hyperlink NavigateUri="https://github.com/keni2006/Axis2"
                                   RequestNavigate="Hyperlink_RequestNavigate"
                                   Foreground="{DynamicResource AccentBrush}" TextDecorations="None">
                            🔗 github.com/keni2006/Axis2
                        </Hyperlink>
                    </TextBlock>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
