<UserControl x:Class="Axis2.WPF.Views.SpawnTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Axis2.WPF.Views"
             xmlns:models="clr-namespace:Axis2.WPF.Models"
             xmlns:converters="clr-namespace:Axis2.WPF.Converters"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="1200">
    <UserControl.Resources>
        <converters:IsSelectedPathConverter x:Key="IsSelectedPathConverter" />
        <converters:ImageScaleConverter x:Key="ImageScaleConverter" />
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" MinWidth="150"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="3*" MinWidth="200"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="3*" MinWidth="200"/>
            <ColumnDefinition Width="250"/>
        </Grid.ColumnDefinitions>

        <!-- TreeView for Categories -->
        <TreeView Grid.Column="0" ItemsSource="{Binding Categories}"
                  VirtualizingStackPanel.IsVirtualizing="True"
                  VirtualizingStackPanel.VirtualizationMode="Recycling"
                  ScrollViewer.CanContentScroll="True">
            <TreeView.Resources>
                <HierarchicalDataTemplate DataType="{x:Type models:Category}" ItemsSource="{Binding SubSections}">
                    <TextBlock Text="{Binding Name}" FontWeight="Bold"/>
                </HierarchicalDataTemplate>
                <DataTemplate DataType="{x:Type models:SubCategory}">
                    <TextBlock Text="{Binding Name}"/>
                </DataTemplate>
                <Style TargetType="{x:Type TreeViewItem}">
                    <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}" />
                    <EventSetter Event="PreviewMouseLeftButtonDown" Handler="OnTreeViewItemPreviewMouseLeftButtonDown" />
                    <Style.Triggers>
                        <!-- Category Coloring -->
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding IsExpanded, RelativeSource={RelativeSource Self}}" Value="True" />
                                <Condition Value="True">
                                    <Condition.Binding>
                                        <MultiBinding Converter="{StaticResource IsSelectedPathConverter}">
                                            <Binding Path="DataContext" RelativeSource="{RelativeSource Self}" />
                                            <Binding Path="DataContext.SelectedCategoryInTree" RelativeSource="{RelativeSource AncestorType={x:Type UserControl}}" />
                                        </MultiBinding>
                                    </Condition.Binding>
                                </Condition>
                            </MultiDataTrigger.Conditions>
                            <Setter Property="Foreground" Value="Blue" />
                        </MultiDataTrigger>
                        <!-- SubCategory Coloring -->
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding IsExpanded, RelativeSource={RelativeSource Self}}" Value="True" />
                                <Condition Value="True">
                                    <Condition.Binding>
                                        <MultiBinding Converter="{StaticResource IsSelectedPathConverter}">
                                            <Binding Path="DataContext" RelativeSource="{RelativeSource Self}" />
                                            <Binding Path="DataContext.SelectedSubCategoryInTree" RelativeSource="{RelativeSource AncestorType={x:Type UserControl}}" />
                                        </MultiBinding>
                                    </Condition.Binding>
                                </Condition>
                            </MultiDataTrigger.Conditions>
                            <Setter Property="Foreground" Value="Green" />
                        </MultiDataTrigger>
                    </Style.Triggers>
                </Style>
            </TreeView.Resources>
        </TreeView>

        <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Center" VerticalAlignment="Stretch"/>

        <!-- Search + ListView for NPCs -->
        <Grid Grid.Column="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Border Grid.Row="0" Margin="0,0,0,4" Padding="8,5" CornerRadius="6"
                    Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="🔍" VerticalAlignment="Center" Margin="2,0,8,0" FontSize="14"/>
                    <TextBox Grid.Column="1" VerticalAlignment="Center" BorderThickness="0" Background="Transparent"
                             Text="{Binding InlineSearchText, UpdateSourceTrigger=PropertyChanged, Delay=150}"
                             ToolTip="Search all NPCs by name or hex body id (live)"/>
                </Grid>
            </Border>
            <ListView Grid.Row="1" ItemsSource="{Binding DisplayedItems}" SelectedItem="{Binding SelectedItem}">
                <ListView.View>
                    <GridView AllowsColumnReorder="True">
                        <GridViewColumn Header="NPC/Group" DisplayMemberBinding="{Binding Description}" Width="150"/>
                        <GridViewColumn Header="ID" DisplayMemberBinding="{Binding Id}" Width="80"/>
                    </GridView>
                </ListView.View>
            </ListView>
        </Grid>

        <GridSplitter Grid.Column="3" Width="5" HorizontalAlignment="Center" VerticalAlignment="Stretch"/>

        <!-- New ListView for Spawn Group Members -->
        <GroupBox Grid.Column="4" Header="Group Content">
            <ListView x:Name="SpawnGroupMembersListView"
                      ItemsSource="{Binding SpawnGroupMembers}"
                      SelectedItem="{Binding SelectedMember}"
                      SelectionChanged="SpawnGroupMembersListView_SelectionChanged">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="NPC" DisplayMemberBinding="{Binding Npc.Description}" Width="150"/>
                        <GridViewColumn Header="Amount" DisplayMemberBinding="{Binding Amount}" Width="50"/>
                    </GridView>
                </ListView.View>
            </ListView>
        </GroupBox>

        <!-- Right Panel for Preview and Commands -->
        <Grid Grid.Column="5">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <GroupBox Header="NPC View" Grid.Row="0">
                <DockPanel>
                    <Slider DockPanel.Dock="Bottom" Minimum="0" Maximum="4" Value="{Binding Direction}" IsSnapToTickEnabled="True" TickFrequency="1"/>
                    <Border x:Name="ImageBorder" BorderBrush="Black" BorderThickness="1" Height="150" Margin="5">
                        <Image Source="{Binding ItemImage}" Stretch="None" HorizontalAlignment="Center" VerticalAlignment="Center">
                            <Image.LayoutTransform>
                                <ScaleTransform>
                                    <ScaleTransform.ScaleX>
                                        <MultiBinding Converter="{StaticResource ImageScaleConverter}">
                                            <Binding Path="ItemImage"/>
                                            <Binding Path="ActualWidth" ElementName="ImageBorder"/>
                                            <Binding Path="ActualHeight" ElementName="ImageBorder"/>
                                        </MultiBinding>
                                    </ScaleTransform.ScaleX>
                                    <ScaleTransform.ScaleY>
                                        <MultiBinding Converter="{StaticResource ImageScaleConverter}">
                                            <Binding Path="ItemImage"/>
                                            <Binding Path="ActualWidth" ElementName="ImageBorder"/>
                                            <Binding Path="ActualHeight" ElementName="ImageBorder"/>
                                        </MultiBinding>
                                    </ScaleTransform.ScaleY>
                                </ScaleTransform>
                            </Image.LayoutTransform>
                        </Image>
                    </Border>
                </DockPanel>
            </GroupBox>

            <StackPanel Grid.Row="1">
                <Button Content="Summon" Command="{Binding SummonCommand}"/>
                <Button Content="Find" Click="FindButton_Click"/>
                <Button Content="Remove" Command="{Binding RemoveCommand}"/>
                <Button Content="Shrink" Command="{Binding ShrinkCommand}"/>
                <Button Content="Freeze" Command="{Binding FreezeCommand}"/>
                <GroupBox Header="Set Homedist">
                    <DockPanel>
                        <TextBox DockPanel.Dock="Right" Text="{Binding HomeDist}" Width="50"/>
                        <Button Content="Set Home" Command="{Binding SetHomeCommand}"/>
                        <Button Content="Set Homedist" Command="{Binding SetHomeDistCommand}"/>
                    </DockPanel>
                </GroupBox>
                <GroupBox Header="NPC Spawn">
                    <StackPanel>
                        <DockPanel>
                            <TextBlock Text="Amount"/>
                            <TextBox Text="{Binding Amount}" Width="50"/>
                        </DockPanel>
                        <DockPanel>
                            <TextBlock Text="Min Time"/>
                            <TextBox Text="{Binding MinTime}" Width="50"/>
                        </DockPanel>
                        <DockPanel>
                            <TextBlock Text="Max Time"/>
                            <TextBox Text="{Binding MaxTime}" Width="50"/>
                        </DockPanel>
                        <DockPanel>
                            <TextBlock Text="Max Dist"/>
                            <TextBox Text="{Binding MaxDist}" Width="50"/>
                        </DockPanel>
                        <Button Content="Place" Command="{Binding PlaceSpawnCommand}"/>
                        <Button Content="Init" Command="{Binding InitSpawnCommand}"/>
                    </StackPanel>
                </GroupBox>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>