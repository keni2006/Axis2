<UserControl x:Class="Axis2.WPF.Views.SettingsTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Axis2.WPF.Views"
             xmlns:views="clr-namespace:Axis2.WPF.Views.Settings"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000">
    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="5">
        <StackPanel Margin="5">
            <!-- CLIENT | UO FILE PATHS (side by side, matching the design) -->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="1.3*"/>
                </Grid.ColumnDefinitions>
                <GroupBox Grid.Column="0" Header="Client">
                    <views:SettingsGeneralView DataContext="{Binding SettingsGeneralViewModel}"/>
                </GroupBox>
                <GroupBox Grid.Column="1" Header="UO File Paths">
                    <views:SettingsFilePathsView DataContext="{Binding SettingsFilePathsViewModel}"/>
                </GroupBox>
            </Grid>

            <!-- ADVANCED — the extra settings groups the design doesn't show, kept so nothing is lost -->
            <GroupBox Header="Advanced">
                <TabControl Margin="0,4,0,0">
                    <TabItem Header="Item Tab">
                        <views:SettingsItemTabView DataContext="{Binding SettingsItemTabViewModel}"/>
                    </TabItem>
                    <TabItem Header="Travel Tab">
                        <views:SettingsTravelTabView DataContext="{Binding SettingsTravelTabViewModel}"/>
                    </TabItem>
                    <TabItem Header="Spawn Tab">
                        <views:SettingsSpawnTabView DataContext="{Binding SettingsSpawnTabViewModel}"/>
                    </TabItem>
                    <TabItem Header="Override Paths">
                        <views:SettingsOverridePathsView DataContext="{Binding SettingsOverridePathsViewModel}"/>
                    </TabItem>
                </TabControl>
            </GroupBox>

            <!-- COMPACT PANEL — which tabs are available in compact (mini) mode -->
            <GroupBox Header="Compact Panel — tabs shown in mini mode">
                <WrapPanel>
                    <CheckBox Content="General"  Margin="6,4,20,4" IsChecked="{Binding DataContext.CompactShowGeneral,  RelativeSource={RelativeSource AncestorType={x:Type Window}}, Mode=TwoWay}"/>
                    <CheckBox Content="Item"     Margin="6,4,20,4" IsChecked="{Binding DataContext.CompactShowItem,     RelativeSource={RelativeSource AncestorType={x:Type Window}}, Mode=TwoWay}"/>
                    <CheckBox Content="Spawn"    Margin="6,4,20,4" IsChecked="{Binding DataContext.CompactShowSpawn,    RelativeSource={RelativeSource AncestorType={x:Type Window}}, Mode=TwoWay}"/>
                    <CheckBox Content="Travel"   Margin="6,4,20,4" IsChecked="{Binding DataContext.CompactShowTravel,   RelativeSource={RelativeSource AncestorType={x:Type Window}}, Mode=TwoWay}"/>
                    <CheckBox Content="Misc"     Margin="6,4,20,4" IsChecked="{Binding DataContext.CompactShowMisc,     RelativeSource={RelativeSource AncestorType={x:Type Window}}, Mode=TwoWay}"/>
                    <CheckBox Content="Commands" Margin="6,4,20,4" IsChecked="{Binding DataContext.CompactShowCommands, RelativeSource={RelativeSource AncestorType={x:Type Window}}, Mode=TwoWay}"/>
                    <CheckBox Content="Account"  Margin="6,4,20,4" IsChecked="{Binding DataContext.CompactShowAccount,  RelativeSource={RelativeSource AncestorType={x:Type Window}}, Mode=TwoWay}"/>
                </WrapPanel>
            </GroupBox>
        </StackPanel>
    </ScrollViewer>
</UserControl>
